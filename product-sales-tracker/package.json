{
  "name": "product-sales-tracker",
  "version": "1.0.0",
  "main": "dist/main/main/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.renderer.json && cp src/renderer/index.html dist/renderer/ && cp src/renderer/login.html dist/renderer/ && cp src/renderer/styles.css dist/renderer/ && cp src/renderer/login-styles.css dist/renderer/ && cp src/renderer/login.js dist/renderer/ && cp src/renderer/secret-window.html dist/renderer/ && cp src/renderer/secret-window.js dist/renderer/ && mkdir -p dist/renderer/translations && cp src/renderer/translations/*.json dist/renderer/translations/",
    "start": "npm run build && electron ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A focused Electron application for tracking products and sales without customer management. This application allows you to manage your product inventory and record sales transactions, with daily reporting capabilities.",
  "dependencies": {
    "chart.js": "^4.5.1",
    "electron": "^39.2.1",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "concurrently": "^9.2.1",
    "electron-builder": "^26.0.12",
    "typescript": "^5.9.3"
  }
}